@model List<ProductModel>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible fade show">
        <i class="fas fa-check-circle me-2"></i>
        @TempData["SuccessMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}
@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show">
        <i class="fas fa-exclamation-triangle me-2"></i>
        @TempData["ErrorMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <div class="pagetitle">
        <h1><i class="fas fa-box text-success me-2"></i>Product Management</h1>
        <nav>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="@Url.Action("Index", "Home")" class="text-success">Home</a></li>
                <li class="breadcrumb-item">Tables</li>
                <li class="breadcrumb-item active">Product Data</li>
            </ol>
        </nav>
    </div>
    <div>
        <a asp-action="AddEdit" asp-controller="Product" class="btn btn-success">
            <i class="fas fa-plus me-2"></i>Add Product
        </a>
    </div>
</div>

<section class="section">
    <div class="row">
        <div class="col-lg-12">
            <div class="card shadow-sm border-success">
                <div class="card-header bg-success text-white">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-boxes me-2"></i>Product Inventory
                    </h5>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped table-hover datatable">
                            <thead class="table-success">
                                <tr>
                                    <th><i class="fas fa-id-badge me-1"></i>Product ID</th>
                                    <th><i class="fas fa-shopping-bag me-1"></i>Product Name</th>
                                    <th><i class="fas fa-tag me-1"></i>Category</th>
                                    <th><i class="fas fa-dollar-sign me-1"></i>Price</th>
                                    <th><i class="fas fa-chart-line me-1"></i>Used Stock</th>
                                    <th><i class="fas fa-warehouse me-1"></i>Remaining Stock</th>
                                    <th class="text-center"><i class="fas fa-cogs me-1"></i>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model != null && Model.Any())
                                {
                                    foreach (var item in Model)
                                    {
                                        <tr>
                                            <td><span class="badge bg-success">#@item.ProductID</span></td>
                                            <td class="fw-bold text-dark">@item.ProductName</td>
                                            <td><span class="badge bg-info">@item.CategoryName</span></td>
                                            <td><span class="text-primary fw-bold">$@item.Price.ToString("0.00")</span></td>
                                            <td>
                                                <span class="badge bg-success">@item.UsedStock</span>
                                            </td>
                                            <td>
                                                <span class="badge bg-success">@item.RemainingStock</span>
                                            </td>
                                            <td class="text-center">
                                                <a asp-action="AddEdit" asp-controller="Product" asp-route-id="@item.ProductID" class="btn btn-sm btn-outline-success me-1" title="Edit">
                                                    <i class="fas fa-edit"></i>
                                                </a>
                                                <a asp-action="Delete" asp-controller="Product" asp-route-id="@item.ProductID" class="btn btn-sm btn-outline-danger" title="Delete" onclick="return confirm('Are you sure you want to delete this product?');">
                                                    <i class="fas fa-trash"></i>
                                                </a>
                                            </td>
                                        </tr>
                                    }
                                }
                                else
                                {
                                    <tr>
                                        <td colspan="7" class="text-center text-muted py-4">
                                            <i class="fas fa-boxes fs-1 text-muted mb-3 d-block"></i>
                                            <h5>No products found</h5>
                                            <p class="text-muted">Start by adding your first product to the inventory.</p>
                                            <a asp-action="AddEdit" asp-controller="Product" class="btn btn-success">
                                                <i class="fas fa-plus me-2"></i>Add First Product
                                            </a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>